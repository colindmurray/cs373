language: python

python:
    - "3.4"

before_install:
    - sudo apt-get update

install:
    - sudo apt-get install nodejs
    - sudo apt-get install npm
    - sudo apt-get install python-numpy
    - sudo npm     install

script:
    - cd javascript
    - make test
    - cd -
    - cd python
    - make test
