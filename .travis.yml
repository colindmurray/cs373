language: python

python:
    - "3.4"

before_install:
    - sudo apt-get update

install:
    - sudo apt-get install python-numpy
    - node -v
    - sudo apt-get install nodejs
    - node -v
    - npm install npm -g
    - npm install lodash

script:
    - cd javascript
    - make test
    - cd -
    - cd python
    - make test
